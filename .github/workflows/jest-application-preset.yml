name: jest-application-preset

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          cache: yarn
          check-latest: true
          node-version: 'lts/*'
      - name: Install dependencies
        run: yarn install --immutable

  npm:
    if: ${{ github.event_name == 'push' }}
    needs: install
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          cache: yarn
          check-latest: true
          node-version: 'lts/*'
      - name: Publish
        working-directory: packages/jest-application-preset
        run: >
          yarn config set npmAuthToken $NPM_AUTH_TOKEN;
          yarn npm publish --tolerate-republish;
        env:
          NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}

on:
  # pull_request:
  #   branches: [main]
  #   paths:
  #     - '.github/workflows/jest-application-preset.yml'
  #     - 'packages/jest-application-preset/**/*'
  #     - 'packages/jest-preset/**/*'
  #     - 'packages/jest-transformer/**/*'
  push:
    branches: [main]
    paths:
      - '.github/workflows/jest-application-preset.yml'
      - 'packages/jest-application-preset/**/*'
      - 'packages/jest-preset/**/*'
      - 'packages/jest-transformer/**/*'
